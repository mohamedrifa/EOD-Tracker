import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFFF7A00);
  static const background = Color(0xFFF5F5F5);

  static const completed = Colors.green;
  static const inProgress = Colors.grey;
  static const pending = Colors.red;
}